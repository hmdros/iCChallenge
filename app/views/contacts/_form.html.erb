<%= simple_form_for(@contact) do |f| %>
  <div class="col">

    <div class="form-field">
      <%= f.input :name %>
    </div>
    <div class="form-field">
      <%= f.input :email %>
    </div>

    <div class="form-actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
